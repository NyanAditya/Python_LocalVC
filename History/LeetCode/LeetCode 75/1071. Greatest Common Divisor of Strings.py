def gcdOfStrings(self, str1: str, str2: str) -> str:
    l1 = len(str1)
    l2 = len(str2)

    if str1.count(str2) != 0:
        i = max(l1, l2)
        if l1 >= l2:
            div = str2

        else:
            div = str2


    else:
        return ''
